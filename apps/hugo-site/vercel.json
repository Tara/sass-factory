{
  // "buildCommand": "cd apps/hugo-site && hugo --gc --minify",
  // "outputDirectory": "apps/hugo-site/public",
  "framework": "hugo",
   "build": {
        "env": {
          "HUGO_VERSION": "0.138.0"
        }
      }  
}