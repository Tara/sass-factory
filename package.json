{
  "name": "improv-team-workspace",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "hugo:dev": "cd apps/hugo-site && hugo server",
    "hugo:build": "cd apps/hugo-site && hugo"
  }
}
